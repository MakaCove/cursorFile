# 示例

## 示例 A：科目类型维护（精简片段）

### 输出格式示意

表格列：**模块 | ID | 标题 | 优先级 | 类型 | 前置条件 | 步骤 | 测试数据 | 预期结果 | 备注/覆盖点**

| 模块 | ID | 标题 | 优先级 | 类型 | 前置条件 | 步骤 | 测试数据 | 预期结果 | 备注/覆盖点 |
|------|-----|------|--------|------|----------|------|----------|----------|-------------|
| 科目类型维护 | TC-001 | 新增上级科目类型成功 | P0 | 功能 | 当前用户有新增权限 | 1. 点击“新增” 2. 上级科目类型选“顶级” 3. 科目类型名称输入系统中不存在的名称 4. 点击“保存” | 科目类型名称=资产类-测试01 | 1）保存成功，弹窗关闭 2）列表中新增一条：上级为空或“顶级”，科目类型名称为输入值，状态默认“启用” | 主流程 |
| 科目类型维护 | TC-002 | 新增下级科目类型成功 | P0 | 功能 | 存在已启用的上级科目类型 | 1. 点击“新增” 2. 上级科目类型选择某已启用上级 3. 科目类型名称输入唯一名称 4. 点击“保存” | 上级=资产类；名称=流动资产类-子01 | 1）保存成功并关闭弹窗 2）列表中新增一条，上级列显示所选名称，科目类型名称列显示新名称，状态默认“启用” | 主流程 |
| 科目类型维护 | TC-008 | 编辑操作同新增一致 | P0 | 功能 | 编辑时必填、唯一性等校验与新增相同 | 同新增校验项 | — | 保存校验规则与新增一致（必填、名称唯一等） | 编辑校验 |
| 科目类型维护 | TC-022 | 日志信息展示 | P1 | 功能 | — | 1. 点击某行“操作日志”按钮 | — | 日志详情按记录时间**倒序**显示 | 操作日志展示 |
| 科目类型维护 | TC-025 | 页面查询操作 | P1 | 功能 | — | 1. 上级科目类型：多选，支持模糊搜索 2. 科目类型：多选，支持模糊搜索 3. 状态：多选 4. 选择筛选条件后点击查询 | 任选组合 | 列表刷新，展示符合筛选条件的数据（多条件取并集） | 查询 |
| 科目类型维护 | TC-026 | 页面查询重置操作 | P1 | 功能 | — | 1. 点击重置按钮 | — | 筛选条件清空，列表刷新展示所有数据 | 重置 |
| 科目类型维护 | TC-024 | 无权限按钮接口操作权限控制 | P1 | 权限 | 使用无该操作权限的账号或 Token | 1. 通过接口调用（如新增/编辑/启用/禁用） | — | 接口响应返回无权限异常信息 | 接口权限 |

## 示例 B：合并科目维护（含查询/重置/日志/备注/接口权限时的写法示例）

| 模块 | ID | 标题 | 优先级 | 类型 | 前置条件 | 步骤 | 测试数据 | 预期结果 | 备注/覆盖点 |
|------|-----|------|--------|------|----------|------|----------|----------|-------------|
| 合并科目维护 | TC-029 | 新增时科目类型模糊搜索 | P1 | 功能 | 科目类型维护中已有数据 | 1. 点击“新增” 2. 在科目类型输入框中输入已存在/不存在的关键字 | 关键字=资产 | 搜索结果展示符合条件的数据；无匹配则展示为空 | 下拉模糊搜索 |
| 合并科目维护 | TC-037 | 编辑操作同新增一致 | P0 | 功能 | 编辑保存校验与新增相同 | 同新增（必填、编码唯一、名称唯一） | — | 保存校验规则与新增一致 | 编辑校验 |
| 合并科目维护 | TC-049 | 日志信息展示 | P1 | 功能 | — | 1. 点击操作日志按钮 | — | 日志详情按记录时间倒序显示 | 操作日志展示 |
| 合并科目维护 | TC-052 | 页面查询操作 | P1 | 功能 | — | 1. 合并科目编码：文本输入框，支持模糊搜索 2. 合并科目名称：文本输入框，支持模糊搜索 3. 状态：选择 4. 选择完成后点击查询 | 任选组合 | 列表刷新，展示符合筛选条件的数据（并集） | 查询 |
| 合并科目维护 | TC-053 | 页面查询重置操作 | P1 | 功能 | — | 1. 点击重置按钮 | — | 筛选条件清空，列表刷新展示所有数据 | 重置 |
| 合并科目维护 | TC-054 | 备注编辑 | P1 | 功能 | — | 1. 点击备注编辑按钮 2. 在输入框中输入内容 3. 保存 | 备注=测试备注内容 | 备注列展示最新输入/修改的数据，备注可随时编辑 | 备注编辑 |
| 合并科目维护 | TC-051 | 无权限按钮接口操作权限控制 | P1 | 权限 | 无该操作权限 | 1. 通过接口调用 | — | 接口响应返回无权限异常信息 | 接口权限 |

## 说明

- 以上仅为**格式与写法示例**：当某页面实际具备查询、重置、操作日志、备注编辑、接口权限等能力时，可参考表中步骤/预期的写法；若页面或需求中没有这些能力，则不必生成对应用例。
- **编辑操作同新增一致**：当需求明确“编辑保存校验同新增”时，可输出一条此类用例，步骤/预期可简写或引用新增；再视需要补充“编辑不同状态成功”“编辑时唯一性校验”等。
- 步骤与预期中的控件名称、提示文案、展示顺序等均应以**当前截图与需求**为准，不套用固定话术。
